# encoding:utf-8def script_method(fn, _rcb=None):    return fndef script(obj, optimize=True, _frames_up=0, _rcb=None):    return objimport torch.jitscript_method1 = torch.jit.script_methodscript1 = torch.jit.scripttorch.jit.script_method = script_methodtorch.jit.script = scriptfrom createflask import start_serverfrom utils.tools import read_check_yamlfrom multiprocessing import freeze_supportif __name__ == "__main__":    freeze_support()  # 防止多开    flag, param = read_check_yaml("./init_config.yaml")    start_server(param) if flag else print("config file does not exist/missing config param!")